{
  "name": "@vibe-code/server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --external zod --external playwright && bun -e \"import fs from 'node:fs'; fs.mkdirSync('dist/templates', { recursive: true }); fs.cpSync('src/agents/templates', 'dist/templates', { recursive: true })\"",
    "typecheck": "tsc --noEmit",
    "test": "bun test src --timeout 15000"
  },
  "dependencies": {
    "@vibe-code/shared": "workspace:*",
    "croner": "^10.0.1",
    "dotenv": "^17.4.2",
    "hono": "^4.12.26",
    "playwright": "^1.61.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "fast-check": "^4.8.0",
    "typescript": "^5.8.3"
  }
}
